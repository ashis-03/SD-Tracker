from django.urls import path
from . import views

urlpatterns = [
    # path('automation_logs/', views.automation_logs, name='automation_logs'),
    # path('workflow_detail/', views.workflow_detail, name='workflow_detail'),
    # path('workflow_list/', views.workflow_list, name='workflow_list'),
]